# Implementation Plan

- [x] 1. Establish the structural migration baseline
  - [x] 1.1 Run the existing backend typecheck and focused contracts for the first affected boundaries; capture the exact passing baseline before moving any exports.
    - Cover bootstrap/composition, persistence, middleware, translation transport, and console contracts already present in `test/`.
    - _Requirements: 3.4, 7.2_
  - [x] 1.2 Add or update only contract tests needed to lock the observable behavior of an existing boundary before its source owner moves.
    - Do not introduce tests for file paths, implementation details, or refactor plumbing.
    - _Requirements: 3.1, 3.3, 7.2_

- [x] 2. Relocate application authentication and split bootstrap assembly
  - [x] 2.1 Move `src/auth/**` to `src/application/auth/**` in one clean cutover.
    - Migrate every production and test importer with LSP, remove the root `auth/` directory, preserve OAuth/session/token/quota behavior, and commit this relocation before any bootstrap extraction.
    - _Requirements: 2.1-2.5, 3.1-3.4, 7.1-7.2, 8.1-8.11_
  - [x] 2.2 Extract provider registry/model metadata assembly into `bootstrap/registry.ts` and route resolver/revision/request-log assembly into `bootstrap/routing.ts`.
    - Preserve `createCartethyiaRuntime` as the public bootstrap façade and migrate all direct importers in the same change.
    - _Requirements: 1.1, 2.1, 3.1, 3.3, 7.1_
  - [x] 2.3 Extract OAuth, quota, and recovery worker construction into `bootstrap/workers.ts`; retain the exact refresh timing, failure policy, and shutdown handles.
    - _Requirements: 1.1, 3.4, 7.2_
  - [x] 2.4 Extract console assembly into `bootstrap/console.ts` and deterministic shutdown/scheduled maintenance into `bootstrap/lifecycle.ts`.
    - Keep `bootstrap/composition.ts` as thin dependency assembly with one construction owner for every long-lived runtime dependency.
    - _Requirements: 1.1, 2.1, 7.2, 8.2, 8.9_
  - [x] 2.5 Apply the naming contract to every moved bootstrap symbol, migrate all LSP-discovered importers, remove moved implementation from the façade, and verify middleware contracts plus backend typecheck.
    - _Requirements: 3.1, 3.2, 7.1, 7.2, 8.1-8.10_

- [ ] 3. Decompose configuration persistence by durable aggregate
  - [x] 3.1 Extract config database lifecycle and row/domain mappings from `storage/main/config.ts` into `database.ts` and `mappers.ts`.
    - Preserve lazy opening, pragmas, schema initialization, existing-column repair, singleton behavior, and test reset semantics.
    - _Requirements: 1.1, 4.1, 4.3, 4.4_
  - [ ] 3.2 Move settings, API-key, account, route-health, proxy, provider-model, routing, custom-provider, filter-rule, share-link, IP-ban, and Warp-account repositories into their named `storage/main/repositories/` owners.
    - Keep SQL, table names, cache behavior, and mutation semantics unchanged; no ORM or schema migration.
    - _Requirements: 1.1, 1.2, 3.1, 4.1-4.4_
  - [ ] 3.3 Move durable route-health, account-health, model-lock, quota-state, OAuth-token, credential-config, and proxy-pool stores into `storage/main/stores.ts`.
    - _Requirements: 4.1, 4.3_
  - [ ] 3.4 Create `storage/main/persistence.ts` as the sole `createConfigPersistence` owner, update `storage/index.ts` to expose only the explicit public persistence contract, and delete the old implementation path.
    - _Requirements: 2.1, 3.1-3.3, 4.1, 8.2, 8.9_
  - [ ] 3.5 Migrate all production/test importers with LSP, rename moved symbols to the repository-wide contract, and verify persistence, share-link, and Warp lifecycle contracts plus backend typecheck.
    - _Requirements: 3.1-3.3, 4.3, 7.1-7.2, 8.1-8.11_

- [ ] 4. Decompose runtime telemetry persistence by data lifecycle
  - [ ] 4.1 Extract runtime contracts, database lifecycle, and write buffering into `storage/runtime/contracts.ts`, `database.ts`, and `write-buffer.ts`.
    - Retain prepared statement reuse, write batch thresholds, retry behavior, process-local cache behavior, and test reset semantics.
    - _Requirements: 1.1, 4.2-4.4_
  - [ ] 4.2 Move request history/aggregates, payload artifacts, console logs, Warp metrics, and retention into their named runtime modules.
    - Preserve SQL table names, cursor semantics, console-log push/filter behavior, bounded retention, and payload redaction/truncation rules.
    - _Requirements: 1.1, 3.1, 4.2-4.4_
  - [ ] 4.3 Create `storage/runtime/persistence.ts` as the only `createRuntimePersistence` owner, remove `runtime.ts`, and migrate every production and test importer.
    - _Requirements: 2.1, 3.1-3.3, 4.2, 8.2, 8.9_
  - [ ] 4.4 Apply canonical telemetry/log naming, then verify persistence, logging, and middleware contracts plus backend typecheck.
    - _Requirements: 4.2-4.3, 7.1-7.2, 8.1-8.11_

- [ ] 5. Split console DTOs and domain services
  - [ ] 5.1 Move console DTOs from `console/views.ts` into domain-owned `console/views/*.ts` modules for auth, accounts, providers, models, proxies, routing, settings, and telemetry.
    - Keep request/response field names, secret redaction, and external console API bodies unchanged.
    - _Requirements: 1.1-1.2, 3.1, 5.3, 5.4, 8.1, 8.11_
  - [ ] 5.2 Move auth/API-key, provider/model, account/OAuth/quota, proxy, routing, settings/filter-rule, telemetry, and backup services into `console/services/*.ts` modules.
    - `console/services/composition.ts` remains the sole `createConsoleServices` owner; individual services receive explicit repositories/dependencies.
    - _Requirements: 1.1, 3.1-3.3, 5.1, 5.4, 8.2, 8.8-8.9_
  - [ ] 5.3 Move console repository factory helpers to canonical `createConsole…Repository` names and preserve their one-time construction in the console composition owner.
    - _Requirements: 5.1, 7.1, 8.2, 8.9-8.10_
  - [ ] 5.4 Migrate all importers, remove `console/views.ts` and `console/services.ts`, and verify console, CLI, OAuth, proxy, and routing contracts plus backend typecheck.
    - _Requirements: 3.1-3.3, 5.1-5.4, 7.1-7.2, 8.1-8.11_

- [ ] 6. Split console HTTP route registration
  - [ ] 6.1 Extract auth, account, provider, model, proxy, routing, settings, telemetry, backup, and stream route groups from `console/api.ts` into named `console/api/*-routes.ts` modules.
    - Each module exports `register<Domain>Routes` and receives an explicit route-dependencies object; it creates no long-lived dependency.
    - _Requirements: 1.1-1.2, 3.1, 5.2, 5.4, 8.3, 8.9_
  - [ ] 6.2 Create `console/api/index.ts` as the sole `createConsoleApi` composition entrypoint and move only shared API construction there.
    - Construct each console sub-application once and retain its owner/lifecycle contract.
    - _Requirements: 3.1-3.3, 5.2, 8.2-8.4, 8.9_
  - [ ] 6.3 Rename the session guard, traffic response helpers, and response presentation functions according to the naming contract without changing route behavior.
    - _Requirements: 5.4, 8.2-8.7, 8.10_
  - [ ] 6.4 Migrate every route importer, delete `console/api.ts`, and verify middleware contracts, authenticated console API smoke coverage, backend typecheck, and dashboard build.
    - _Requirements: 3.1-3.4, 5.2, 7.1-7.2, 8.1-8.11_

- [ ] 7. Split the proxy request lifecycle
  - [ ] 7.1 Extract request contracts and deterministic preparation into `application/request/contracts.ts` and `prepare.ts`.
    - Preserve validation, normalization, tool-call safety, RTK/headroom ordering, and admission token estimation.
    - _Requirements: 1.1, 3.1, 6.2-6.3_
  - [ ] 7.2 Extract bounded payload capture and telemetry projection into `payload-capture.ts` and `telemetry.ts`.
    - Preserve privacy mode, payload bounds, redaction, request-log event shape, and final telemetry records.
    - _Requirements: 1.1, 3.1, 6.2-6.3_
  - [ ] 7.3 Extract route candidate execution/failover classification into `route-attempt.ts` and make `execute.ts` the sole `runProxyRequest` owner.
    - Preserve stream ownership, abort propagation, retry policy, route transitions, permits, and provider error meaning.
    - _Requirements: 3.1-3.4, 6.2-6.3, 8.4, 8.9_
  - [ ] 7.4 Migrate all production and test importers, delete `application/request.ts`, and verify middleware, routing-cache, core-policy, session-IP, RTK, headroom contracts plus backend typecheck.
    - _Requirements: 3.1-3.3, 6.2-6.3, 7.1-7.2, 8.1-8.11_

- [ ] 8. Decompose Open SSE shared transport by protocol lifecycle
  - [ ] 8.1 Extract shared transport types and provider/upstream error mapping into `transport/contracts.ts` and `errors.ts`.
    - Keep one canonical owner for conversion to `ProviderCallError` and rename error factories/converters to the naming contract.
    - _Requirements: 1.1, 3.1, 6.1, 6.3, 8.2, 8.5-8.7_
  - [ ] 8.2 Extract abort coordination, bounded body reads, outbound fetch, SSE decoding, stream mapping, and model catalog construction into their named transport modules.
    - Preserve timeout/abort causality, redirect/SSRF enforcement, byte limits, terminal event behavior, and catalog semantics.
    - _Requirements: 1.1, 3.1, 6.1, 6.3, 8.2, 8.5-8.7_
  - [ ] 8.3 Extract OpenAI-compatible adapter construction into `transport/openai-adapter.ts`; rename it `createOpenAIAdapter` and migrate every provider adapter importer.
    - _Requirements: 3.1-3.3, 6.1, 8.2, 8.10_
  - [ ] 8.4 Rename protocol stream mappers to provider/surface-specific `create…StreamMapper` names, preserving one new mutable mapper per upstream stream.
    - _Requirements: 6.3, 8.2, 8.9-8.10_
  - [ ] 8.5 Delete `open-sse/transport/shared.ts`, migrate all direct importers/tests, and verify transport/protocol translation contracts plus backend typecheck.
    - _Requirements: 3.1-3.3, 6.1, 7.1-7.2, 8.1-8.11_

- [ ] 9. Review remaining high-LOC cohesive domains and complete naming cutover
  - [ ] 9.1 Audit `traffic/network.ts` and `application/auth/credentials.ts` against the ownership contract; split only where a cohesive domain boundary exists.
    - Preserve network policy, proxy selection, OAuth credential selection, account health, refresh, and failure semantics.
    - _Requirements: 1.1-1.4, 3.1-3.4, 7.1-7.2_
  - [ ] 9.2 Apply the naming contract to every remaining `src/**/*.ts` symbol, including error factories, formatters, parsers, coercion helpers, routing helpers, provider-facing types, callback names, and boundary field mappers.
    - Keep third-party wire names confined to their provider/codec/OAuth boundary; migrate each exported rename with LSP and leave no alias.
    - _Requirements: 3.1-3.3, 8.1-8.11_
  - [ ] 9.3 Add an AST/LSP-backed source naming audit that rejects prohibited identifiers and stale renamed symbols, without enforcing third-party wire field names outside their permitted boundaries.
    - _Requirements: 7.2, 8.1-8.11_
  - [ ] 9.4 Verify targeted network, auth, OAuth, and naming-audit tests plus backend typecheck.
    - _Requirements: 7.1-7.2, 8.1-8.11_

- [ ] 10. Verify the final clean source tree
  - [ ] 10.1 Run an LSP workspace reference/diagnostic audit for moved exports and confirm old modules, aliases, forwarding files, and prohibited generic module names are absent.
    - _Requirements: 1.2-1.4, 2.1-2.4, 3.1-3.3, 8.1-8.11_
  - [ ] 10.2 Run the complete backend suite, dashboard suite, backend typecheck, dashboard build, and native backend build.
    - _Requirements: 7.3_
  - [ ] 10.3 Review the final diff for unintended HTTP, schema, backup, environment, provider behavior, or dashboard feature changes; remove any structural leftovers.
    - _Requirements: 3.4, 4.3-4.4, 5.4, 6.3, 7.3_
