{
  "name": "cartethyia",
  "version": "2.0.0-beta",
  "description": "OpenAI <-> Anthropic compat reverse proxy (Chat Completions, Responses, Messages) with prompt-cache passthrough and image decode.",
  "private": true,
  "type": "module",
  "module": "src/main.ts",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "build": "bun build --compile --minify --bytecode --target bun --outfile bin/cartethyia src/main.ts",
    "test": "bun test --cwd test . ../src/application ../src/security ../src/traffic ../src/providers --timeout 60000",
    "test:coverage": "bun test --cwd test . ../src/application ../src/security ../src/traffic ../src/providers --coverage --timeout 60000",
    "bench": "bun test --timeout 60000 test/bench",
    "bench:baseline": "bun test --timeout 60000 test/bench/benchmark-edge-cases.test.ts"
  },
  "dependencies": {
    "@bufbuild/protobuf": "2.13.0",
    "elysia": "^1.4.29",
    "socks-proxy-agent": "^10.1.0"
  },
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "2.13.0",
    "@types/bun": "latest",
    "typescript": "5"
  }
}
