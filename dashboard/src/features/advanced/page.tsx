import { CustomizationPage } from "../customization/page";

/** Customization page — appearance, background, sidebar icon controls. */
export function AdvancedPage() {
  return (
    <div className="dashboard-page">
      <CustomizationPage />
    </div>
  );
}
