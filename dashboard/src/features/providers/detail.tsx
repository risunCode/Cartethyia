/* @jsxImportSource solid-js */

import { useInfiniteQuery, useQuery, useQueryClient } from "@tanstack/solid-query";
import { ArrowLeft, ArrowUpDown, Bot, Download, ExternalLink, FileUp, FlaskConical, Info, LockOpen, Pencil, Plus, PowerOff, RefreshCw, Trash2, Users, Copy } from "lucide-solid";
import { For, Show, createEffect, createMemo, createSignal, onCleanup } from "solid-js";
import { A, useLocation, useParams } from "@solidjs/router";
import { toast } from "../../lib/toast";
import { consoleDelete, consoleGet, consolePatch, consolePost } from "../../lib/console-api";
import { sanitizeErrorMessage } from "../../lib/api";
import { qk } from "../../lib/query-keys";
import { cn } from "../../lib/cn";
import { formatDuration, formatTokens } from "../../lib/format";
import { formatModelPricing, type ModelPricing } from "./formatters";
import { errorMessage, selectAccountTestModel } from "./detail-helpers";
import { OAuthConnectActions, type OAuthFlowCapabilities } from "./oauth-connect-actions";
import { createVirtualList } from "../../lib/virtual-list";
import { Skeleton } from "../../components/ui/badge";
import { Button } from "../../components/ui/button";
import { Card, CardHeader } from "../../components/ui/card";
import { Dialog } from "../../components/ui/dialog";
import { Input, Label } from "../../components/ui/input";
import { StatusDot } from "../../components/status-dot";
import { ProviderIcon } from "../../components/provider-icon";
import { Switch } from "../../components/ui/switch";
import { ConfirmDialog } from "../../components/shared";
import { formatAccountHealthAccessibleStatus, formatAccountHealthStatus, formatHealthAccessibleStatus, formatRouteHealthStatus, healthPollingInterval, type AccountHealthSnapshot, type RouteHealthSnapshot } from "../../lib/account-health";

interface ModelMetadataResponse { context?: { inputTokens?: number | null; outputTokens?: number | null }; categories?: readonly ("vision" | "text" | "reasoning")[]; pricing?: { inputPerMillion?: number | null; outputPerMillion?: number | null }; source?: "catalog" | "custom"; }
export type ProviderCapability = "chat" | "media" | "websearch";
const PROVIDER_CAPABILITY_LABELS: Record<ProviderCapability, string> = { chat: "Chat", media: "Image / Video", websearch: "Web Search" };
interface ModelEntry { id: string; reasoning?: boolean; vision?: boolean; websearch?: boolean; capabilities: Record<ProviderCapability, boolean>; contextWindow?: number; maxOutputTokens?: number; enabled: boolean; source: "built-in" | "manual" | "imported"; pricing?: ModelPricing; }
interface QuotaSnapshot { readonly status: "unknown" | "refreshing" | "ready" | "error"; readonly remaining: number | null; readonly limit: number | null; readonly resetsAt: string | null; readonly error: string | null; }
interface AccountEntry { id: string; provider: string; name: string; credentialKind: string; credentialHint: string; active: boolean; health: (AccountHealthSnapshot & { occurredAt?: string | null; lastRefreshAt?: string | null }) | null; quota?: QuotaSnapshot | null; }
interface RouteState { id?: string; routeId?: string; name?: string; label?: string; health?: RouteHealthSnapshot | null; status?: RouteHealthSnapshot["status"]; failureKind?: string | null; statusCode?: number | null; sanitizedMessage?: string | null; retryAt?: string | null; }
interface RouteSwitchEvent { scope?: "account" | "proxy"; previousRouteId?: string | null; replacementRouteId?: string | null; reason?: string; occurredAt?: string; }
interface ProviderDetail { id: string; name: string; icon: string; authKind: "none" | "session" | "oauth" | "api-key"; supportsOAuth: boolean; oauthFlows: OAuthFlowCapabilities; credentialKinds: string[]; authHint: string; credentialUrl: string | null; accountCredentialKind: string; prefix: string; models: ModelEntry[]; accounts: AccountEntry[]; routing: { strategy: "priority" | "round-robin"; stickyLimit: number; useStickyLimit: boolean; proxyRouteId: string | null }; modelManagement: { canAddModels: boolean; canFetchModels: boolean }; status: "ok" | "warn"; usageToday: number | null; health: RouteHealthSnapshot | null; proxyHealth: RouteHealthSnapshot | null; failedRoute: RouteState | null; replacementRoute: RouteState | null; switchEvent: RouteSwitchEvent | null; }
interface ProviderDetailResponse { id: string; name: string; protocol: string; credentialKind: "api_key" | "oauth" | "session" | "manual" | "none"; credentialKinds?: Array<"api_key" | "oauth" | "session" | "manual" | "none">; credentialUrl?: string | null; oauthFlows?: OAuthFlowCapabilities; enabled: boolean; models: Array<{ modelId: string; displayName?: string; enabled: boolean; source?: "built-in" | "manual" | "imported"; capabilities?: { chat?: boolean; media?: boolean; websearch?: boolean }; metadata?: ModelMetadataResponse }>; modelManagement?: { canAddModels: boolean; canFetchModels: boolean }; accounts: Array<{ id: string; provider?: string; providerId?: string; name?: string; label?: string; credentialKind?: string; credentialHint?: string | null; enabled?: boolean; active?: boolean; health?: unknown; quota?: unknown }>; routing?: { strategy?: "priority" | "round-robin"; stickyLimit?: number; useStickyLimit?: boolean; proxyRouteId?: string | null }; }

function normalizeHealth(value: unknown): AccountEntry["health"] { if (typeof value !== "object" || value === null || Array.isArray(value)) return null; const raw = value as Record<string, unknown>; const status = raw.status; if (status !== "healthy" && status !== "refreshing" && status !== "error" && status !== "disabled" && status !== "reauthentication-required" && status !== "cooling_down") return null; const text = (candidate: unknown): string | null => typeof candidate === "string" && candidate.length <= 240 ? candidate : null; return { status, errorKind: text(raw.errorKind), failureKind: text(raw.failureKind), statusCode: typeof raw.statusCode === "number" ? raw.statusCode : null, sanitizedMessage: text(raw.sanitizedMessage), retryAt: text(raw.retryAt), ...(text(raw.occurredAt) ? { occurredAt: text(raw.occurredAt) } : {}), ...(text(raw.lastRefreshAt) ? { lastRefreshAt: text(raw.lastRefreshAt) } : {}) }; }
function normalizeQuota(value: unknown): QuotaSnapshot | null { if (typeof value !== "object" || value === null || Array.isArray(value)) return null; const raw = value as Record<string, unknown>; const status = raw.status === "refreshing" || raw.status === "ready" || raw.status === "error" ? raw.status : "unknown"; const numberValue = (candidate: unknown) => typeof candidate === "number" && Number.isFinite(candidate) ? candidate : null; const stringValue = (candidate: unknown) => typeof candidate === "string" && candidate.length <= 240 ? candidate : null; return { status, remaining: numberValue(raw.remaining ?? raw.remainingPercent), limit: numberValue(raw.limit), resetsAt: stringValue(raw.resetsAt), error: stringValue(raw.error ?? raw.sanitizedError) }; }
function normalizeAccount(value: ProviderDetailResponse["accounts"][number], providerId: string): AccountEntry | null { if (typeof value.id !== "string" || !value.id) return null; const name = typeof value.name === "string" && value.name.trim() ? value.name.trim() : typeof value.label === "string" && value.label.trim() ? value.label.trim() : value.id; return { id: value.id, provider: value.providerId ?? value.provider ?? providerId, name, credentialKind: typeof value.credentialKind === "string" ? value.credentialKind : "unknown", credentialHint: typeof value.credentialHint === "string" && value.credentialHint.length <= 128 ? value.credentialHint : "", active: value.active ?? value.enabled === true, health: normalizeHealth(value.health), quota: normalizeQuota(value.quota) }; }
export function normalizeProviderDetail(response: ProviderDetailResponse): ProviderDetail { const authKind = response.credentialKind === "api_key" ? "api-key" : response.credentialKind === "manual" ? "none" : response.credentialKind; const supportsOAuth = response.credentialKinds?.includes("oauth") ?? response.credentialKind === "oauth"; const oauthFlows = response.oauthFlows ?? { browser: false, device: false }; return { id: response.id, name: response.name, icon: response.id, authKind, supportsOAuth, oauthFlows, credentialKinds: response.credentialKinds ?? [response.credentialKind], authHint: authKind === "none" ? "No authentication required" : supportsOAuth ? "Use an API key or connect an OAuth account." : "Add an account credential to route requests.", credentialUrl: response.credentialUrl ?? null, accountCredentialKind: response.credentialKinds?.includes("api_key") ? "api_key" : response.credentialKind, prefix: response.id, models: response.models.map((model) => { const metadata = model.metadata; const categories = metadata?.categories ?? []; const input = metadata?.pricing?.inputPerMillion ?? null; const output = metadata?.pricing?.outputPerMillion ?? null; return { id: model.modelId, enabled: model.enabled, source: model.source ?? (metadata?.source === "custom" ? "manual" : "built-in"), reasoning: categories.includes("reasoning"), vision: categories.includes("vision"), websearch: model.capabilities?.websearch === true, capabilities: { chat: model.capabilities?.chat === true, media: model.capabilities?.media === true, websearch: model.capabilities?.websearch === true }, contextWindow: metadata?.context?.inputTokens ?? undefined, maxOutputTokens: metadata?.context?.outputTokens ?? undefined, pricing: input !== null && output !== null ? { input, output } : undefined }; }), modelManagement: response.modelManagement ?? { canAddModels: true, canFetchModels: true }, status: response.enabled && (authKind === "none" || response.accounts.some((account) => account.active ?? account.enabled === true)) ? "ok" : "warn", usageToday: null, accounts: response.accounts.flatMap((account) => { const item = normalizeAccount(account, response.id); return item ? [item] : []; }), routing: { strategy: response.routing?.strategy === "round-robin" ? "round-robin" : "priority", stickyLimit: response.routing?.stickyLimit ?? 1, useStickyLimit: response.routing?.useStickyLimit ?? false, proxyRouteId: response.routing?.proxyRouteId ?? null }, health: null, proxyHealth: null, failedRoute: null, replacementRoute: null, switchEvent: null }; }

interface OAuthLoginStart { sessionId: string; provider: string; status: string; authorizationUrl: string; redirectUri: string; instructions: string; expiresAt: number; userCode?: string | null; verificationUri?: string | null; intervalSeconds?: number | null; flow?: "browser" | "device"; }
interface OAuthLoginStatus { sessionId: string; provider: string; status: string; accountId?: string; errorKind?: string; errorMessage?: string; expiresAt: number; }
interface TestResult { resolveOk: boolean; latencyMs: number; firstVisibleTextMs?: number; ok: boolean; error?: string; }
async function probeModel(provider: string, model: string, credentialMode: "auto" | "account", accountId?: string): Promise<TestResult> { const result = await consolePost<{ ok: boolean; latencyMs: number; firstVisibleTextMs?: number; error?: { message?: string } }>("/tools/probe", { provider, model, credentialMode, ...(accountId ? { accountId } : {}) }); return { resolveOk: true, ok: result.ok, latencyMs: Number.isFinite(result.latencyMs) ? result.latencyMs : 0, firstVisibleTextMs: result.firstVisibleTextMs, error: result.ok ? undefined : sanitizeErrorMessage(result.error?.message, "request failed") }; }

function AccountDialog(props: { providerId: string; account: AccountEntry | null; expectedKind: string; onClose: () => void; onSaved: () => void }) { const [name, setName] = createSignal(props.account?.name ?? `${props.providerId}-${Date.now() % 1000}`); const [credentialRef, setCredentialRef] = createSignal(""); const [busy, setBusy] = createSignal(false); const save = async () => { if (!name().trim() || (!props.account && !credentialRef().trim())) return; setBusy(true); try { if (props.account) await consolePost(`/providers/${encodeURIComponent(props.providerId)}/accounts/${encodeURIComponent(props.account.id)}`, { label: name().trim(), ...(credentialRef().trim() ? { credentialRef: credentialRef().trim() } : {}), enabled: true }); else await consolePost(`/providers/${encodeURIComponent(props.providerId)}/accounts`, { label: name().trim(), credentialRef: credentialRef().trim(), credentialKind: props.expectedKind, enabled: true }); toast.success(props.account ? "Account updated" : "Account added"); props.onSaved(); props.onClose(); } catch (error) { toast.error(errorMessage(error)); } finally { setBusy(false); } }; return <Dialog open onClose={props.onClose} title={props.account ? `Edit ${props.account.name}` : "Add connection"} footer={<><Button variant="secondary" onClick={props.onClose}>Cancel</Button><Button disabled={busy() || !name().trim() || (!props.account && !credentialRef().trim())} onClick={() => void save()}>{busy() ? "Saving…" : "Save"}</Button></>}><div class="space-y-3"><div><Label>Name</Label><Input value={name()} onInput={(event) => setName(event.currentTarget.value)} /></div><div><Label>Opaque credentialRef ({props.expectedKind})</Label><textarea value={credentialRef()} onInput={(event) => setCredentialRef(event.currentTarget.value)} class="min-h-24 w-full rounded-xl border border-[var(--inner-border)] bg-[var(--input-bg)] px-3 py-2 font-mono text-xs" spellcheck={false} /></div></div></Dialog>; }
function RouteNotice(props: { title: string; route: RouteState; tone: "failed" | "replacement" }) { const health = props.route.health ?? (props.route.status ? { status: props.route.status, failureKind: props.route.failureKind ?? null, statusCode: props.route.statusCode ?? null, sanitizedMessage: props.route.sanitizedMessage ?? null, retryAt: props.route.retryAt ?? null } : null); const label = props.route.label ?? props.route.name ?? props.route.routeId ?? props.route.id ?? "unknown route"; return <div class={cn("rounded-xl border px-3 py-2.5", props.tone === "failed" ? "border-[var(--red)]/35 bg-[var(--red)]/5" : "border-[var(--green)]/35 bg-[var(--green)]/5")}><div class="text-[10px] font-semibold uppercase tracking-wide text-[var(--text-3)]">{props.title}</div><div class="mt-1 truncate text-xs font-semibold">{label}</div><div class={cn("mt-0.5 truncate text-[10px]", props.tone === "failed" ? "text-[var(--red)]" : "text-[var(--green)]")} aria-label={`${props.title} health: ${formatHealthAccessibleStatus(health) ?? "No health details"}`}>{formatRouteHealthStatus({ health }) ?? "No health details"}</div></div>; }

export function ProviderDetailPage() {
  const params = useParams<{ id: string }>(); const location = useLocation(); const queryClient = useQueryClient(); const id = () => params.id;
  const [accountModal, setAccountModal] = createSignal<AccountEntry | null>(null); const [addAccount, setAddAccount] = createSignal(false); const [selected, setSelected] = createSignal<string[]>([]); const [sort, setSort] = createSignal<"name" | "status">("name"); const [oauthSession, setOauthSession] = createSignal<OAuthLoginStart | null>(null); const [callbackValue, setCallbackValue] = createSignal(""); const [deleteConfirm, setDeleteConfirm] = createSignal<string | null>(null); const [deleteFetchedConfirm, setDeleteFetchedConfirm] = createSignal(false); const [addModel, setAddModel] = createSignal(false); const [newModel, setNewModel] = createSignal(""); const [pageVisible, setPageVisible] = createSignal(true);
  const detail = useQuery(() => ({ queryKey: qk.provider.detail(id()), queryFn: async () => normalizeProviderDetail(await consoleGet<ProviderDetailResponse>(`/providers/${encodeURIComponent(id() ?? "")}`)), enabled: Boolean(id()), refetchOnWindowFocus: true }));
  const accountsQuery = useInfiniteQuery(() => ({ queryKey: qk.provider.accounts(id()), queryFn: async ({ pageParam }) => { const cursor = typeof pageParam === "string" && pageParam ? `?cursor=${encodeURIComponent(pageParam)}` : ""; const response = await consoleGet<{ items: ProviderDetailResponse["accounts"]; nextCursor?: string | null }>(`/providers/${encodeURIComponent(id() ?? "")}/accounts${cursor}`); return { items: response.items.flatMap((account) => { const value = normalizeAccount(account, id() ?? ""); return value ? [value] : []; }), nextCursor: response.nextCursor ?? null }; }, initialPageParam: null as string | null, getNextPageParam: (page) => page.nextCursor ?? undefined, enabled: Boolean(id()), refetchInterval: healthPollingInterval(pageVisible()) }));
  const data = createMemo(() => detail.data); const accounts = createMemo(() => accountsQuery.data?.pages.flatMap((page) => page.items) ?? data()?.accounts ?? []); const sortedAccounts = createMemo(() => [...accounts()].sort((a, b) => sort() === "name" ? a.name.localeCompare(b.name) : Number(b.active) - Number(a.active) || a.name.localeCompare(b.name))); const accountWindow = createVirtualList(sortedAccounts, 56); const fetchedModels = createMemo(() => (data()?.models ?? []).filter((model) => model.source !== "built-in")); const selectedCapability = createMemo<ProviderCapability | null>(() => { const value = new URLSearchParams(location.search).get("capability"); return value === "chat" || value === "media" || value === "websearch" ? value : null; }); const visibleModels = createMemo(() => { const capability = selectedCapability(); return (data()?.models ?? []).filter((model) => capability === null || model.capabilities[capability]); });
  const handledAction = createSignal<string | null>(null);
  const invalidate = () => { void queryClient.invalidateQueries({ queryKey: qk.provider.detail(id()) }); void queryClient.invalidateQueries({ queryKey: qk.provider.accounts(id()) }); void queryClient.invalidateQueries({ queryKey: qk.catalog.providers }); };
  const startOAuth = async (flow: "browser" | "device") => { try { const session = await consolePost<OAuthLoginStart>(`/auth/oauth/start?providerId=${encodeURIComponent(id() ?? "")}`, { flow, scopes: [] }); setOauthSession(session); if (flow === "browser") window.open(session.authorizationUrl, "cartethyia-oauth", "popup,width=720,height=820"); } catch (error) { toast.error(errorMessage(error)); } };
  createEffect(() => { const provider = data(); const action = new URLSearchParams(location.search).get("action"); if (!provider || (action !== "oauth" && action !== "json")) return; const key = `${provider.id}:${action}:${new URLSearchParams(location.search).get("kind") ?? ""}`; if (handledAction[0]() === key) return; handledAction[1](key); if (action === "oauth") { const flow = provider.oauthFlows.browser ? "browser" : provider.oauthFlows.device ? "device" : null; if (flow) void startOAuth(flow); } else setAddAccount(true); });
  const oauthStatus = useQuery(() => ({ queryKey: qk.oauthLogin.session(oauthSession()?.sessionId), queryFn: () => consoleGet<OAuthLoginStatus>(`/auth/oauth/sessions/${encodeURIComponent(oauthSession()?.sessionId ?? "")}`), enabled: Boolean(oauthSession()?.sessionId), refetchInterval: pageVisible() ? 2000 : false }));
  createEffect(() => { const status = oauthStatus.data?.status; if (status === "completed") { toast.success("OAuth account connected"); invalidate(); setOauthSession(null); setCallbackValue(""); } else if (status === "failed" || status === "expired" || status === "cancelled") { toast.error(oauthStatus.data?.errorMessage ?? `OAuth authorization ${status}`); setOauthSession(null); } });
  createEffect(() => { const onVisibility = () => setPageVisible(!document.hidden); document.addEventListener("visibilitychange", onVisibility); onCleanup(() => document.removeEventListener("visibilitychange", onVisibility)); });
  const completeOAuth = async () => { const session = oauthSession(); const value = callbackValue().trim(); if (!session || !value) return; let code = value; let state: string | undefined; try { const url = new URL(value); code = url.searchParams.get("code") ?? value; state = url.searchParams.get("state") ?? undefined; } catch { /* raw code accepted */ } try { await consolePost(`/auth/oauth/sessions/${encodeURIComponent(session.sessionId)}/complete`, { code, ...(state ? { state } : {}) }); await oauthStatus.refetch(); } catch (error) { toast.error(errorMessage(error)); } };
  const deleteAccount = async (accountId: string) => { try { await consolePost(`/providers/${encodeURIComponent(id() ?? "")}/accounts/batch-delete`, { items: [accountId] }); toast.success("Account deleted"); invalidate(); } catch (error) { toast.error(errorMessage(error)); } };
  const setAccountsActive = async (active: boolean) => { const ids = selected(); if (!ids.length) return; try { await consolePatch(`/providers/${encodeURIComponent(id() ?? "")}/accounts/batch`, { items: ids.map((accountId) => ({ accountId, enabled: active })) }); toast.success(`${ids.length} account${ids.length === 1 ? "" : "s"} ${active ? "enabled" : "disabled"}`); setSelected([]); invalidate(); } catch (error) { toast.error(errorMessage(error)); } };
  const toggleModel = async (model: ModelEntry) => { try { await consolePatch(`/providers/${encodeURIComponent(id() ?? "")}/models/${encodeURIComponent(model.id)}`, { enabled: !model.enabled }); invalidate(); } catch (error) { toast.error(errorMessage(error)); } };
  const removeModel = async (model: ModelEntry) => { try { if (model.source === "built-in") await consolePatch(`/providers/${encodeURIComponent(id() ?? "")}/models/${encodeURIComponent(model.id)}`, { enabled: false }); else await consoleDelete(`/providers/${encodeURIComponent(id() ?? "")}/models/${encodeURIComponent(model.id)}`); toast.success(model.source === "built-in" ? "Model disabled" : "Custom model removed"); invalidate(); } catch (error) { toast.error(errorMessage(error)); } };
  const testAccount = async (account: AccountEntry) => { const model = selectAccountTestModel(id() ?? "", data()?.models ?? []); if (!model) { toast.error("No model is available to test this connection."); return; } try { const result = await probeModel(id() ?? "", model.id, "account", account.id); toast[result.ok ? "success" : "error"](`${account.name} · ${result.ok ? formatDuration(result.latencyMs) : result.error ?? "failed"}`); invalidate(); } catch (error) { toast.error(errorMessage(error)); } };
  if (!id()) return null;
  return <Show when={!detail.isPending && data()} fallback={<div class="space-y-3"><Skeleton className="h-8 w-48" /><Skeleton className="h-64" /></div>}><Show when={!detail.isError} fallback={<Card className="text-center"><p class="py-8 text-sm text-[var(--text-2)]">Failed to load this provider.</p><Button variant="secondary" onClick={() => void detail.refetch()}>Retry</Button></Card>}><div class="dashboard-page space-y-4"><div><A href={selectedCapability() ? `/providers?capability=${selectedCapability()}` : "/providers"} class="inline-flex items-center gap-1.5 text-[11.5px] font-medium text-[var(--text-2)]"><ArrowLeft size={13} /> Back to Providers</A><div class="mt-3 flex items-center gap-3.5"><ProviderIcon icon={data()!.icon} name={data()!.name} size={48} /><div class="min-w-0"><div class="flex flex-wrap items-center gap-2.5"><h1 class="text-xl font-bold tracking-tight">{data()!.name}</h1><button type="button" onClick={() => void detail.refetch()} aria-label="Refresh"><RefreshCw size={14} /></button><code class="rounded bg-[var(--accent-soft)] px-1.5 py-0.5 font-mono text-[11px] font-semibold text-[var(--accent)]">{data()!.prefix}</code><Show when={data()!.credentialUrl}><a href={data()!.credentialUrl!} target="_blank" rel="noreferrer" class="inline-flex items-center gap-1 text-[11.5px] text-[var(--accent)]"><ExternalLink size={12} /> Get API Key</a></Show></div><div class="mt-1 flex items-center gap-2 text-[11.5px] text-[var(--text-2)]"><StatusDot status={data()!.status} />{accounts().filter((account) => account.active).length} connections</div></div></div></div><Card className="py-3.5"><div class="flex items-start gap-3"><span class={cn("mt-0.5 grid h-7 w-7 place-items-center rounded-[9px]", data()!.authKind === "none" ? "bg-[rgba(48,209,88,0.14)] text-[var(--green)]" : "bg-[rgba(100,210,255,0.15)] text-[var(--teal)]")}>{data()!.authKind === "none" ? <LockOpen size={14} /> : <Info size={14} />}</span><p class="text-[11.5px] text-[var(--text-2)]">{data()!.authHint}</p></div></Card><Show when={data()!.failedRoute || data()!.replacementRoute}><section class="grid grid-cols-1 gap-2 sm:grid-cols-2"><Show when={data()!.failedRoute}><RouteNotice title="Failed route" route={data()!.failedRoute!} tone="failed" /></Show><Show when={data()!.replacementRoute}><RouteNotice title="Replacement route" route={data()!.replacementRoute!} tone="replacement" /></Show></section></Show><Card className="space-y-4"><CardHeader title="Accounts" icon={Users} sub={`${accounts().length} account${accounts().length === 1 ? "" : "s"}`}><div class="flex flex-wrap gap-1.5"><OAuthConnectActions flows={data()!.oauthFlows} onStart={(flow) => void startOAuth(flow)} /><Button size="sm" onClick={() => setAddAccount(true)}><Plus size={13} /> New account</Button></div></CardHeader><Show when={selected().length > 0}><div class="flex flex-wrap items-center gap-2 rounded-xl bg-[var(--accent-soft)] px-3 py-2.5"><span class="text-xs font-semibold">{selected().length} selected</span><Button size="sm" variant="secondary" onClick={() => void setAccountsActive(true)}>Enable</Button><Button size="sm" variant="secondary" onClick={() => void setAccountsActive(false)}>Disable</Button><Button size="sm" variant="ghost" onClick={() => setSelected([])}>Clear</Button></div></Show><div class="overflow-hidden rounded-xl border border-[var(--inner-border)]"><Show when={accounts().length > 0} fallback={<div class="py-8 text-center text-xs text-[var(--text-3)]">No connections yet — add one to start routing requests.</div>}><div ref={accountWindow.containerRef} onScroll={(event) => { const element = event.currentTarget; if (element.scrollHeight - element.scrollTop - element.clientHeight < 200 && accountsQuery.hasNextPage && !accountsQuery.isFetchingNextPage) void accountsQuery.fetchNextPage(); }} class="max-h-[21rem] overflow-auto"><table class="w-full text-left text-[11px]"><thead class="sticky top-0 z-10 bg-[var(--hover)] text-[10px] font-semibold uppercase tracking-wide text-[var(--text-3)]"><tr><th class="w-8 px-2 py-2.5"><input type="checkbox" aria-label="Select all accounts" checked={selected().length === accounts().length} onChange={(event) => setSelected(event.currentTarget.checked ? accounts().map((account) => account.id) : [])} /></th><th class="px-2 py-2.5"><button type="button" onClick={() => setSort(sort() === "name" ? "status" : "name")} class="inline-flex items-center gap-1">Account <ArrowUpDown size={11} /></button></th><th class="px-2 py-2.5 text-right">Actions</th></tr></thead><tbody><Show when={accountWindow.topPadding() > 0}><tr aria-hidden="true" style={{ height: `${accountWindow.topPadding()}px` }}><td colSpan={3} /></tr></Show><For each={accountWindow.visibleItems()}>{(account) => <tr class="border-t border-[var(--inner-border)]"><td class="px-2 py-2.5"><input type="checkbox" checked={selected().includes(account.id)} onChange={(event) => setSelected(event.currentTarget.checked ? [...selected(), account.id] : selected().filter((id) => id !== account.id))} /></td><td class="px-2 py-2.5"><div class="font-semibold">{account.name}</div><div class="text-[10px] text-[var(--text-3)]" title={formatAccountHealthAccessibleStatus(account) ?? "No health details"}>{formatAccountHealthStatus(account) ?? (account.active ? "active" : "disabled")}</div></td><td class="px-2 py-2.5"><div class="flex items-center justify-end gap-1"><Button variant="ghost" size="icon" aria-label={`Test ${account.name}`} onClick={() => void testAccount(account)}><FlaskConical size={13} /></Button><Button variant="ghost" size="icon" aria-label={`Edit ${account.name}`} onClick={() => setAccountModal(account)}><Pencil size={13} /></Button><Button variant="ghost" size="icon" aria-label={`Delete ${account.name}`} onClick={() => setDeleteConfirm(account.id)}><Trash2 size={13} /></Button><Switch checked={account.active} onChange={(active) => { setSelected([account.id]); void setAccountsActive(active); }} label={`${account.active ? "Disable" : "Enable"} ${account.name}`} /></div></td></tr>}</For><Show when={accountWindow.bottomPadding() > 0}><tr aria-hidden="true" style={{ height: `${accountWindow.bottomPadding()}px` }}><td colSpan={3} /></tr></Show></tbody></table></div></Show></div></Card><Card><div class="mb-4 flex items-start justify-between gap-3"><div><div class="text-sm font-bold">{selectedCapability() ? `${PROVIDER_CAPABILITY_LABELS[selectedCapability()!]} Models` : "Available Models"}</div><div class="mt-0.5 text-[11.5px] text-[var(--text-2)]">{visibleModels().filter((model) => model.enabled).length} active · {visibleModels().filter((model) => !model.enabled).length} disabled</div></div><div class="flex flex-wrap gap-1.5"><Show when={data()!.modelManagement.canAddModels}><Button size="sm" variant="secondary" onClick={() => setAddModel(true)}><Plus size={13} /> Add Model</Button></Show><Show when={data()!.modelManagement.canFetchModels}><Button size="sm" variant="secondary" onClick={async () => { try { await consolePost(`/providers/${id()}/models/fetch`, {}); toast.success("Provider registry catalog synced"); invalidate(); } catch (error) { toast.error(errorMessage(error)); } }}><Download size={13} /> Fetch models</Button></Show><Show when={fetchedModels().length > 0}><Button size="sm" variant="ghost" onClick={() => setDeleteFetchedConfirm(true)}><Trash2 size={13} /> Delete fetched</Button></Show><Button size="sm" variant="secondary" onClick={async () => { try { await consolePost(`/providers/${id()}/models/enabled`, { enabled: false }); invalidate(); } catch (error) { toast.error(errorMessage(error)); } }}><PowerOff size={13} /> Disable all</Button></div></div><div class="grid grid-cols-1 gap-2 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"><For each={visibleModels()}>{(model) => <Card className={cn("flex min-h-[168px] flex-col gap-1.5 rounded-xl p-2.5", !model.enabled && "opacity-65")}><div class="flex min-h-[34px] items-start gap-1.5"><Bot size={14} class="mt-0.5 shrink-0 text-[var(--text-3)]" /><div class="min-w-0 flex-1"><div class="break-all font-mono text-[10px] font-semibold">{data()!.prefix}/{model.id}</div><div class="break-all text-[9px] text-[var(--text-3)]">{model.id}</div></div><Button variant="ghost" size="icon" aria-label={`Copy ${data()!.prefix}/${model.id}`} onClick={() => navigator.clipboard?.writeText(`${data()!.prefix}/${model.id}`)}><Copy size={12} /></Button></div><div class="flex min-h-[18px] flex-wrap gap-1">{model.source !== "built-in" && <FileUp size={11} />}{model.reasoning && <span class="rounded-md bg-[var(--hover)] px-1.5 py-0.5 text-[9px]">Reasoning</span>}{model.vision && <span class="rounded-md bg-[var(--hover)] px-1.5 py-0.5 text-[9px]">Vision</span>}{model.websearch && <span class="rounded-md bg-[var(--hover)] px-1.5 py-0.5 text-[9px]">Web</span>}</div><div class="flex-1 text-[9px] text-[var(--text-2)]">{model.contextWindow ? `${formatTokens(model.contextWindow)} context` : "Context unknown"} · {model.maxOutputTokens ? `${formatTokens(model.maxOutputTokens)} max out` : "max out unknown"}<div>{formatModelPricing(model.pricing) ?? "Pricing unknown"}</div></div><div class="flex gap-1.5"><Show when={model.enabled}><Button variant="secondary" size="sm" onClick={() => void probeModel(id()!, model.id, "auto").then((result) => toast[result.ok ? "success" : "error"](`${model.id} · ${result.ok ? formatDuration(result.latencyMs) : result.error ?? "failed"}`)).catch((error) => toast.error(errorMessage(error)))}><FlaskConical size={10} /> Test</Button></Show><Button variant="secondary" size="sm" onClick={() => void toggleModel(model)}>{model.enabled ? "Disable" : "Enable"}</Button><Button variant="ghost" size="sm" onClick={() => void removeModel(model)}><Trash2 size={10} /> Delete</Button></div></Card>}</For></div></Card>{accountModal() && <AccountDialog providerId={id()!} account={accountModal()} expectedKind={data()!.accountCredentialKind} onClose={() => setAccountModal(null)} onSaved={invalidate} />}{addAccount() && <AccountDialog providerId={id()!} account={null} expectedKind={data()!.accountCredentialKind} onClose={() => setAddAccount(false)} onSaved={invalidate} />}{deleteConfirm() && <ConfirmDialog open onClose={() => setDeleteConfirm(null)} onConfirm={() => void deleteAccount(deleteConfirm()!)} title="Delete account?" message="This action cannot be undone." confirmLabel="Delete" danger />}{oauthSession() && <Dialog open onClose={() => setOauthSession(null)} title={`Connect ${data()!.name}`} footer={<><Button variant="ghost" onClick={() => setOauthSession(null)}>Cancel</Button><Button disabled={!callbackValue().trim()} onClick={() => void completeOAuth()}>Connect</Button></>}><div class="space-y-3"><p class="text-sm">{oauthStatus.data?.status ?? "Waiting for authorization…"}</p><a href={oauthSession()!.authorizationUrl} target="_blank" rel="noreferrer" class="break-all text-xs text-[var(--accent)]">{oauthSession()!.authorizationUrl}</a><textarea value={callbackValue()} onInput={(event) => setCallbackValue(event.currentTarget.value)} placeholder={`${oauthSession()!.redirectUri}?code=…`} class="min-h-16 w-full rounded-xl border border-[var(--inner-border)] bg-[var(--hover)] px-3 py-2 font-mono text-[11px]" /></div></Dialog>}{addModel() && <Dialog open onClose={() => setAddModel(false)} title="Add Custom Model" footer={<><Button variant="ghost" onClick={() => setAddModel(false)}>Cancel</Button><Button disabled={!newModel().trim()} onClick={async () => { try { await consolePost(`/providers/${id()}/models`, { modelId: newModel().trim() }); toast.success("Custom model added"); setNewModel(""); setAddModel(false); invalidate(); } catch (error) { toast.error(errorMessage(error)); } }}>Add Model</Button></>}><Label>Model ID<Input value={newModel()} onInput={(event) => setNewModel(event.currentTarget.value)} /></Label></Dialog>}{deleteFetchedConfirm() && <ConfirmDialog open onClose={() => setDeleteFetchedConfirm(false)} onConfirm={async () => { try { await Promise.all(fetchedModels().map((model) => consoleDelete(`/providers/${id()}/models/${encodeURIComponent(model.id)}`))); toast.success(`Deleted ${fetchedModels().length} fetched models`); invalidate(); } catch (error) { toast.error(errorMessage(error)); } }} title="Delete fetched models?" message={`Delete ${fetchedModels().length} fetched models? Built-in models will stay.`} confirmLabel="Delete" danger />}</div></Show></Show>;
}
